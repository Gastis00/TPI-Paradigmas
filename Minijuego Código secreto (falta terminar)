|opciones codigo1 codigo2 jugador1 jugador2 cantRegular1 cantRegular2 cantAciertos1 cantAciertos2 acerto1 acerto2 rta1 rta2 intentos1 intentos2 numAcertados1 numAcertados2 empate pos|

acerto1 := false.
cantAciertos1 :=0.
cantRegular1 := 0.
intentos1 :=0.
numAcertados1 :=''.
jugador1 := 1. "id jugador, borrar"

acerto2 := false.
cantAciertos2 :=0.
cantRegular2 := 0.
intentos2 :=1.
numAcertados2 := ''.
jugador2 := 2. "id jugador, borrar"

empate := false.
Transcript clear.
Transcript show: '---CODIGO SECRETO---' ; cr.
Transcript show: 'Debera adivinar un numero de 4 digitos' ; cr.
Transcript show: 'El primero que adivina gana' ; cr.
Transcript show: 'Si ambos adivinan a la vez, se volvera a jugar' ; cr.
Transcript cr.
UIManager default request: 'Presione ENTER cuando esté listo'.

"mientras no empaten"
[empate not] whileTrue: [
	
	"GENERO CODIGO"
   Transcript show: 'Jugador ', (jugador2 asString) , ' ingrese el código que debe adivinar el jugador ', jugador1 asString.
   
	opciones:= (UIManager default chooseFrom: #('Ingresar código por teclado' 'Generar un código random') message: 'SELECCIONE UNA OPCIÓN'  title: 'EL CODIGO SECRETO' ).
	(opciones = 1) ifTrue: [
		
		"falta controlar error con el metodo enteroError"
		codigo1 := (UIManager default request:'Ingrese un código de 4 digitos').
		[(codigo1 size) = 4] whileFalse: [
			codigo1 := (UIManager default request:'Respete el formato, ingrese un código de 4 digitos')
			]. 
		]
	ifFalse:[
		codigo1 := (1000 to: 9999) atRandom. 
		].
	
	opciones := (UIManager default chooseFrom: #('Ingresar código por teclado' 'Generar un código random') message: 'SELECCIONE UNA OPCIÓN'  title: 'EL CODIGO SECRETO' ).
	(opciones = 1) ifTrue: [
		
		"falta controlar error con el metodo enteroError"
		codigo2 := (UIManager default request:'Ingrese un código de 4 digitos').
		[(codigo1 size) = 4] whileFalse: [
			codigo2 := (UIManager default request:'Respete el formato, ingrese un código de 4 digitos')
			]. 
		]
	ifFalse:[
		codigo2 := (1000 to: 9999) atRandom. 
		].
	
	
	"AMBOS JUGADORES PASAN A JUGAR"
	[(cantAciertos1 ~= 4) and: (cantAciertos2 ~= 4)] whileTrue: [
		
		"JUGADOR 1 JUEGA"
		rta1:= (UIManager default request: 'Turno jugador', (jugador1 asString) ,' - Adivine el codigo').
		intentos1:= intentos1 + 1.
		
		"BIEN(en posicion)"
		1 to: codigo1 do: [ :i |
			
			((codigo1 at: i) = (rta1 at: i)) ifTrue: [ 
				cantAciertos1 := cantAciertos1 + 1.
        		numAcertados1:= (numAcertados1,(rta1 at: i) asString). "para dps no contarlos en el regular"
        		].
	
			].
		
		"REGULAR(esta el numero, no en la posicion)"
		1 to: codigo1 do: [ :i|
			'234' includes: $3.
    		numAcertados1 do: [ :j|
				
				"pregunta si no lo acerto en su posicion"
				(((codigo1 at: i) includes: (rta1 at: i)) and: ((rta1 at: i) ~= (numAcertados1 at: j))) ifTrue: [ 
					cantRegular1:= cantRegular1 + 1.
					].
				
				].
			
			].
		
		"JUGADOR 2 JUEGA"
		rta1:= (UIManager default request: 'Turno jugador', (jugador1 asString) ,' - Adivine el codigo').
		
		"BIEN(en posicion)"
		1 to: codigo1 do: [ :i |
			
			((codigo1 at: i) = (rta1 at: i)) ifTrue: [ 
				cantAciertos1 := cantAciertos1 + 1.
        		numAcertados1:= (numAcertados1,(rta1 at: i) asString). "para dps no contarlos en el regular"
        		].
	
			].
		
		"REGULAR(esta el numero, no en la posicion)"
		1 to: codigo1 do: [ :i|
			
    		numAcertados1 do: [ :j|
				
				"pregunta si no lo acerto en su posicion"
				((codigo1 at: i) ~= (numAcertados1 at: j)) ifTrue: [ 
					cantRegular1:= cantRegular1 + 1.
					].
				
				].
			
			].		
		
		"INFORME PARCIAL DE CUANTOS ACERTARON"
		Transcript show: 'Jugador', (jugador1 asString) , 'acerto ' , (cantAciertos1 asString) , ' en su posicion'.
		Transcript show: 'Jugador', (jugador1 asString) , 'acerto ' , (cantRegular1 asString) , ' pero no estan en su posicion'.
		Transcript show: 'Jugador', (jugador2 asString) , 'acerto ' , (cantAciertos2 asString) , ' en su posicion'.
		Transcript show: 'Jugador', (jugador2 asString) , 'acerto ' , (cantRegular2 asString) , ' pero no estan en su posicion'.
		
		].
	
	"si hay empate va a volver arriba"
	()
	
	].
"termina si no hay empate"

Transcript cr.
Transcript show: '-Duelo finalizado-'.
Transcript cr.

(empate)

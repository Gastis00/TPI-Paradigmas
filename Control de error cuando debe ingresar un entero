modoJuego: entrada
	"controla que sea del tipo entero"
	"ingresa con el UIManager, y la variable llama al metodo"
array:= #(1 2 3)

[(entrada isNil)] whileTrue: [ 
	respuesta:= (UIManager default request: 'Error, no selecciono nada vuelva a intentarlo').
	].

[((respuesta asInteger) isInteger) and: (array includes: respuesta)] whileFalse: [ 
	respuesta:= (UIManager default request: 'Error, vuelva a intentarlo').

	[(respuesta isNil)] whileTrue: [ 
		respuesta:= (UIManager default request: 'Error, no selecciono nada vuelva a intentarlo').
		].
	].
^respuesta.

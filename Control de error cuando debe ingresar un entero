respuesta:= (UIManager default request: 'Ingrese numero'). 
"controla si presiona cancel"
[(respuesta isNil)] whileTrue: [ 
	respuesta:= (UIManager default request: 'Error, no selecciono nada vuelva a intentarlo').
	].
"controla que sea entero"
[((respuesta asInteger) isInteger)] whileFalse: [ 
	respuesta:= (UIManager default request: 'Error, vuelva a intentarlo').
	
	"controla si presiona cancel"
	[(respuesta isNil)] whileTrue: [ 
		respuesta:= (UIManager default request: 'Error, no selecciono nada vuelva a intentarlo').
		].
	].
